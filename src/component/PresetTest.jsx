import React from "react";
import { useDndContext, closestCenter, useSensor, useSensors, PointerSensor, DragOverlay, useDndContext}    from "@dnd-kit/core";


export default function usePresetTest() {
  const useCustomDnd = useDndContext() ; 
  const { active, over } = useCustomDnd; 

  const isSpecialDrag  = active?.

  


  return <> </>
}
